<RadzenButton Click="IncrementCount" Text="@($"Current count: {_currentCount}")" />

@code {
    int _currentCount = 0;

    [Parameter]
    public int IncrementAmount { get; set; } = 1;

    void IncrementCount()
    {
        _currentCount += IncrementAmount;
    }
}